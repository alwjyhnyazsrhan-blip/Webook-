[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "webook-platform"
version = "2.0.0"
description = "Webook Platform & Ingestion Automation"
readme = "README.md"
requires-python = ">=3.8"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["apps*", "core*", "database*", "services*"]
